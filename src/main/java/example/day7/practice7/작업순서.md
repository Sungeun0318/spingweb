1. mysql workbench에서 데이터베이스 새성

   drop database if exists practice7;
   create database practice7;
2. application.properties에서 데이터베이스 연동
   spring.datasource.url = jdbc:mysql://localhost:3306/practice7
3. AppStart 클래스 생성
   @SpringBootApplication
   @EnableJpaAuditing
4. 엔티티 설계
   4-1. BaseTime
     @MappedSuperclass
     @Getter
     @EntityListeners( AuditingEntityListener.class )
     @CreatedDate
     @LastModifiedDate
   4-2. 엔티티 연관관계
     Course 과정        Student 학생  
         |                 |
            Enroll 수강정보

5. 리포지토리 생성 
6. 




